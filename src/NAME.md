# 变量、函数、结构体命名规则

## 变量命名规则

- 前端传回的数据都带有pre_前缀，如用户名信息、用户密码将以pre_user_name、pre_user_psw来指代；将用于返回数据都添加ret_前缀，如将登录成功后的验证消息用ret_OK、ret_ERR来指代
- 变量名采用蛇形命名法

## 函数命名规则

- 函数采用大驼峰命名法
- 函数开头要用注释注明传入值、返回值的信息，对于只传入不返回的函数，返回值置NULL

## 结构体命名规则

- 结构体都以t_开头
- 结构体采用蛇形命名法

## 其他命名规则

- 常量采用全大写命名法
